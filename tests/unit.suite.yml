# Codeception Test Suite Configuration
#
# Suite for unit (internal) tests.

class_name: UnitTester
modules:
    enabled:
        - Asserts
        - \Helper\Unit
        - \Helper\ConfigHelper
        - \Helper\MyMongoHelper
    config:
        \Helper\ConfigHelper:
            database_name: test_test
